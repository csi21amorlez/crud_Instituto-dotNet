@model IEnumerable<DALcrudInstituto.Models.AlumnHasAsig>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdAlumnoNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdAsignaturaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdAlumnoNavigation.Apellidos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdAsignaturaNavigation.Nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdRel">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdRel">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdRel">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
